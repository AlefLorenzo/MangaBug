import express from 'express';

const router = express.Router();

// All chapter CRUD is handled via /api/works routes (workController.js)
// This router exists for backward compatibility

export default router;
